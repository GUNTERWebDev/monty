#include "monty.h"
/**
 * f_queue - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void queue(stack_t **stack, unsigned int count)
{
	(void)stack;
	(void)count;
	diff.lifi = 1;
}

#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void stack(stack_t **stack, unsigned int count)
{
	(void)stack;
	(void)count;
	diff.lifi = 0;
}

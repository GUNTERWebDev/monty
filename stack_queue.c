#include "monty.h"
void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	diff.lifi = 0;
}
void queue(stack_t **head, unsigned int count)
{
        (void)head;
        (void)count;
        diff.lifi = 1;
}
